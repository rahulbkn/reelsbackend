name: Keep Transcoder Warm

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping transcoder health endpoint
        run: curl -sSf https://web-based-ipl-team-prediction-4ak8.onrender.com/health > /dev/null
